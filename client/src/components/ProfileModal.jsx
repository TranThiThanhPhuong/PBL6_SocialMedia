import { useSelector } from 'react-redux';

const ProfrleModal = ({ setShowEdit }) => {
    const user = useSelector((state) => state.user.value); // Lấy thông tin người dùng hiện tại từ Redux store
}